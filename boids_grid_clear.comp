#version 450
layout(local_size_x = 256) in;

layout(std430, binding = 3) buffer CellHeads { uint heads[]; };

void main() {
    uint index = gl_GlobalInvocationID.x;
    if (index >= 8000)
        return;
    heads[index] = 0xFFFFFFFF; 
}